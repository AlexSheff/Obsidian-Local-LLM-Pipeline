# Environment variables for Obsidian Local LLM Pipeline
PORT=3000
# Server host binding (localhost only by default for security)
HOST=127.0.0.1
# Note: NODE_ENV=development is only needed for `npm run dev` (set automatically via cross-env)
